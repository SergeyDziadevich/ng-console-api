apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: ng-console
  labels:
    app: redis
    app.kubernetes.io/name: redis
    app.kubernetes.io/part-of: ng-console
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  selector:
    app: redis
