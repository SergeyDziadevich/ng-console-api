apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: ng-console
  labels:
    app: auth-service
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/part-of: ng-console
spec:
  type: ClusterIP
  ports:
    - port: 4001
      targetPort: 4001
      name: tcp-rpc
  selector:
    app: auth-service
