apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: ng-console
  labels:
    app: user-service
    app.kubernetes.io/name: user-service
    app.kubernetes.io/part-of: ng-console
spec:
  type: ClusterIP
  ports:
    - port: 4002
      targetPort: 4002
      name: tcp-rpc
  selector:
    app: user-service
