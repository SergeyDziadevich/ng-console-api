apiVersion: v1
kind: Service
metadata:
  name: shell
  namespace: ng-console
  labels:
    app: shell
    app.kubernetes.io/name: shell
    app.kubernetes.io/part-of: ng-console
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: shell
