apiVersion: v1
kind: Service
metadata:
  name: users-mfe
  namespace: ng-console
  labels:
    app: users-mfe
    app.kubernetes.io/name: users-mfe
    app.kubernetes.io/part-of: ng-console
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: users-mfe
