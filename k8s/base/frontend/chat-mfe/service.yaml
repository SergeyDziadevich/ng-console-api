apiVersion: v1
kind: Service
metadata:
  name: chat-mfe
  namespace: ng-console
  labels:
    app: chat-mfe
    app.kubernetes.io/name: chat-mfe
    app.kubernetes.io/part-of: ng-console
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: chat-mfe
