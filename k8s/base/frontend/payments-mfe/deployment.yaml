apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-mfe
  namespace: ng-console
  labels:
    app: payments-mfe
    app.kubernetes.io/name: payments-mfe
    app.kubernetes.io/part-of: ng-console
    app.kubernetes.io/component: micro-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payments-mfe
  template:
    metadata:
      labels:
        app: payments-mfe
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        runAsGroup: 101
        fsGroup: 101
      containers:
        - name: payments-mfe
          image: ng-console-payments-mfe:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
